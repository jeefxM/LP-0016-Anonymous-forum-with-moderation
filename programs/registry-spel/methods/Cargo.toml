[package]
name = "registry_spel-methods"
version = "0.1.0"
edition = "2021"

[build-dependencies]
risc0-build = "=3.0.5"

[dependencies]
risc0-zkvm = { version = "=3.0.5", features = ["std"] }
registry_spel_core = { path = "../registry_spel_core" }
